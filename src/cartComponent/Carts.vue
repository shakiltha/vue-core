<script setup>
// import Cart from "./Cart.vue";
// import { onMounted, ref } from "vue";
// let cart = ref({});

// onMounted(async () => {
//   try {
//     const res = await fetch("https://dummyjson.com/carts/1");
//     const data = await res.json();
//     cart.value = data;
//     console.log(cart.value.total);
//   } catch (error) {
//     console.log("Error fetching data:", error);
//   }
// });
</script>

<template>
  <!-- <h2 v-if="cart.value && cart.value.total">{{ cart.value.total }}</h2>
  <h2 v-else>Loading cart data....</h2> -->
</template>
<!-- <h2 class="font-bold text-xl my-2">
    Total: <span class="font-normal">{{ cart.value.total }}</span>
  </h2>
  <h2 class="font-bold text-xl my-2">
    Discounted Total:
    <span class="font-normal">{{ cart.value.discountedTotal }}</span>
  </h2>
  <h2 class="font-bold text-xl my-2">
    Total Products:
    <span class="font-normal">{{ cart.value.totalProducts }}</span>
  </h2>
  <h2 class="font-bold text-xl my-2">
    Total Quantity:
    <span class="font-normal">{{ cart.value.totalQuantity }}</span>
  </h2> -->
<!-- <Cart v-for="item in cart.value.products" :key="item.id" :item="item" /> -->
